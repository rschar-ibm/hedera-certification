{
  "name": "certificate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "@hashgraph/hethers": "^1.2.5",
    "@hashgraph/sdk": "^2.19.2",
    "dotenv": "^16.0.1",
    "envfile": "^6.18.0"
  },
  "scripts": {
    "build": "tsc",
    "start:createaccount": "npm run build && node dist/scripts/createAccounts.js",
    "start:tokenService": "npm run build && node dist/scripts/tokenService.js",
    "start:multisignature": "npm run build && node dist/scripts/multiSignature.js",
    "start:consensusService": "npm run build && node dist/scripts/consensusService.js",
    "start:scheduledTransaction": "npm run build && node dist/scripts/scheduledTransaction.js",
    "start:smartContract": "npm run build && node dist/scripts/smartContract.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "hardhat": "^2.12.6",
    "typescript": "^4.9.5"
  }
}
